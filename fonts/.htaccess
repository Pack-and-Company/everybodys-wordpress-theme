# Allow cross domain requests for fonts
# so eveve can load them
<IfModule mod_headers.c>
    <FilesMatch ".(eot|svg|ttf|otf|woff)">
        Header set Access-Control-Allow-Origin "*"
    </FilesMatch>
</IfModule>